const soma = (num1 , num2) => 'número ' + num1 + ' + número ' + num2 + ' = ' + (num1 + num2)
const mult = (num1, num2) => 'número ' + num1 + ' * número ' + num2 + ' = ' + (num1 * num2)
const sub = (num1, num2) => 'número ' + num1 + ' - número ' + num2 + ' = ' + (num1 - num2)
const div = (num1, num2) => 'número ' + num1 + ' / número ' + num2 + ' = ' + (num1 / num2)
module.exports = {
   soma,
   mult,
   div,
   sub
}









/*const soma = ( num1, num2) => num1 + num2
const mult = ( num1, num2) => num1 * num2
const sub = ( num1, num2) => num1 - num2
const div = ( num1, num2) => num1 / num2

const retorna1 = num1 => num1
const retorna2 = num2 => num2

module.exports = {
   soma,
   mult,
   sub,
   div,
   retorna1,
   retorna2
}*/