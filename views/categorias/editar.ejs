<%- include('../header') %>

<div class="container">
    <!-- Content here -->
    <div class="alert alert-primary" role="alert">
        Editar Categoria
    </div> 
    
<form method="POST" action="/categorias/editar/<%= categoria.id %>">
        <div class="form-group">
          <label for="categoria">Categoria</label>
          <input type="text" class="form-control" id="Categoria" name="categoria" value="<%= categoria.categoria %>" >
        </div>
        
        <button type="submit" class="btn btn-primary">Salvar</button>
      </form> 
</div>

 <!-- <form method="POST" action="/categorias/editar/<%= categoria.id %>">
    <input placeholder = "Insira o novo nome da categoria" type="text" name="categoria" value="<%= categoria.categoria %>" />
    <button>Editar</button>
</form>-->
<%- include('../footer') %>